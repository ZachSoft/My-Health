// enmums can't be declared inside a class;
enum Textsizes {
  small,
  medium,
  large,
}

enum OrderStatus {
  processing,
  shipped,
  delivered,
}

enum Paymentmethods {
  payPal,
  visa,
  googlePay,
  applePay,
  masterCard,
  creditCard,
  razorPay,
  payTm,
  payStack
}

enum Genders {
  Male,
  Female,
  Others,
}
